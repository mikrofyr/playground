#!/bin/bash


# 1) Print the last two lines of data.csv (tail)

# 2) Print the first two lines of data.csv (head)

# 3) Print the first two lines of data.csv, excluding the header (head, grep)

# 4) Replace each comma with single whitespace (sed)

# 5) Print second column (sed as above + awk)

# 6) Print description for all cars made in 1999

